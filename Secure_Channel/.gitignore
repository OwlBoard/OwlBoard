# Ignore generated certificates and private keys
# These should be regenerated in each environment for security

# CA private key (CRITICAL - NEVER COMMIT)
ca/ca.key
ca/ca.srl

# Service private keys (NEVER COMMIT)
certs/*/server.key
certs/*/client.key

# Keep the directory structure
!ca/.gitkeep
!certs/.gitkeep
!certs/*/.gitkeep

# Certificate signing requests (temporary files)
*.csr

# Note: Public certificates (*.crt) can be committed if needed,
# but it's recommended to regenerate them in each environment
